/**
 * RETIRED V1 NARRATION STUB
 *
 * The scene copy that used to live here described the historical V1 contract
 * and could silently regenerate a misleading demo. The current launch video is
 * authored in video/finaltab-winner and must use the final V2 storyboard plus
 * timestamped narration/captions from the reviewed HyperFrames workflow.
 *
 * This stub intentionally exits before reading ELEVENLABS_API_KEY or making a
 * network request.
 */

console.error(
  "RETIRED: scripts/voiceover.mjs contains obsolete V1 narration and will not read an API key. " +
    "Generate the approved V2 narration from video/finaltab-winner instead."
);
process.exitCode = 2;
