const message = [
  "Direct narration-provider generation is retired and this command made no provider request.",
  "The five selected changed-scene MP3s are already bound to the three immutable deployment candidates and nine-call accounting in data/narration-generation-ledger.json; no further provider pass is allowed.",
  "After the five local MP3s and their offline alignments exist, run npm run voice:check, npm run voice:sync, then npm run captions.",
].join(" ");

process.stderr.write(`${message}\n`);
process.exitCode = 1;
